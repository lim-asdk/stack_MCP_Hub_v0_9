{
    "input_schema": {
        "required": [
            "query"
        ],
        "properties": {
            "query": {
                "type": "string",
                "description": "Company name or keyword to search (e.g. Apple Inc)"
            }
        }
    },
    "runtime_contract": {
        "reads_file": true,
        "issues_token": false,
        "makes_network_call": true,
        "transport": "REST"
    },
    "outbox_contract": {
        "top_level_keys": [
            "message",
            "status",
            "lim_aplc"
        ],
        "data_shape": [
            "articles"
        ]
    },
    "source_files": {
        "worker": "kernel_parts/worker.py",
        "bridge": "bridge.py",
        "selftest": "selftest.py"
    }
}

